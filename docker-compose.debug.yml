version: '3.4'

services:
  backend:
    image: backend
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes: 
        - static_data:/collect_static # where all static files are moved to by python manage.py collectstatic

volumes: 
    static_data:

